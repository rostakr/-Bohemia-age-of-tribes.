/** Metres, seconds, Y-up; world positions use X/Z for the horizontal plane. */
export const CONFIG = Object.freeze({
  simulationHz: 20,
  maxCatchUpSteps: 5,
  maxFrameDeltaSeconds: 0.25,
  maxPixelRatio: 1.5,
  telemetrySamples: 120,
  diagnosticsRefreshSeconds: 0.25,
});
